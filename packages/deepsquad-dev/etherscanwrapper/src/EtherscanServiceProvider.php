<?php

namespace App\Providers;

use Illuminate\Support\ServiceProvider;

class EtherscanServiceProvider extends ServiceProvider
{
    /**
     * Register services.
     *
     * @return void
     */
    public function register()
    {
        // create merge for config

        // create tiimestamp

        // create and publish migrations

        // load the routes
    }

    /**
     * Bootstrap services.
     *
     * @return void
     */
    public function boot()
    {
        // merge the config form

        //  bind the instance to the app

        // provide an alias for the class
    }
}
